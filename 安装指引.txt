1.安装python环境python-3.11.4-amd64.exe
pip install playwright        
playwright install chromium
pip install lxml
pip install requests
pip install xlwings
pip install Pillow
pip install comtypes
pip install pyperclip
pip install psutil

设置vscode的python环境为3.11
    按下 Ctrl+Shift+P 打开命令面板。
    输入 Python: Select Interpreter 并选择这个选项。
    从列表中选择你想要使用的 Python 解释器。


查看安装的chromium版本，
在路径 C:\Users\jiangshengping\AppData\Local\ms-playwright查看其下面的文件夹如chromium-1194是1194，则将call test.py脚本中的
browser_path = os.path.join(os.getenv('LOCALAPPDATA'), 'ms-playwright', 'chromium-1208', 'chrome-win', 'chrome.exe')
1208改为1194


1.核对有谁取消掉重新做了。2.赞藏评做上了没有。3.做的时间。4.有没有关注。5.自动付款